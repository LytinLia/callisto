# CALLISTO OpenClaw 插件

CALLISTO (Causal And LLM-Level Invocation Sequence Temporal Observer) 安全检测插件，为 OpenClaw 提供实时风险检测和熔断保护。

## 架构

本插件采用 **Plugin + Skill 双模式** 设计：

| 模式 | 触发方式 | 用途 |
|------|----------|------|
| **Plugin** | 自动拦截 | 通过 `before_tool_call` hook 实时检测并阻断高风险操作 |
| **Skill** | 用户主动调用 | 通过 Skill 工具手动扫描会话风险状态 |

两者共用同一个 Python 后端 (`callisto_agent.py`)，确保检测逻辑一致。

## 安装

确保已安装 OpenClaw 和 Python 3.10+：

```bash
# 进入插件目录
cd ~/.openclaw/extensions/callisto-plugin

# 安装 Python 依赖（如果需要完整 CALLISTO 引擎）
pip install -e .

# 或使用虚拟环境（推荐）
python3 -m venv .venv
source .venv/bin/activate
pip install -e .
```

**注意**: 当前插件使用 Fallback 模式运行，即使不安装 CALLISTO Python 包也能正常工作。插件内置了基于规则的检测引擎。

## 配置

### 在 OpenClaw 中启用

插件配置存储在 `~/.openclaw/openclaw.json`：

```json
{
  "plugins": {
    "enabled": true,
    "entries": {
      "callisto-plugin": {
        "config": {}
      }
    },
    "allow": ["callisto-plugin"]
  }
}
```

### 环境变量（可选）

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `CALLISTO_THRESHOLD` | `3` | 熔断阈值（HIGH 风险操作数量） |

## 使用

### Plugin 模式（自动拦截）

Plugin 模式会自动拦截所有工具调用，无需手动操作。

当检测到高风险操作时，会自动阻止并显示原因：

```
阻止：检测到高风险操作：尝试读取敏感内容：.../etc/passwd...
```

当熔断器打开时：

```
阻止：会话已被熔断：达到风险阈值
```

### Skill 模式（手动扫描）

使用 Skill 工具进行手动扫描：

```bash
# 查看安全状态
openclaw callisto_status

# 扫描当前会话
openclaw callisto_scan

# 手动触发熔断
openclaw callisto_block --reason "可疑行为"
```

或在 OpenClaw 会话中直接使用工具调用。

## 检测的攻击类型

| 代码 | 类型 | 说明 | 检测方式 |
|------|------|------|---------|
| A1 | `rate_flood` | 速率洪水检测 | 8 次调用/5 秒 |
| A2 | `privilege_escalation` | 权限升级检测 | 恶意命令模式匹配 |
| A3 | `data_exfil` | 数据外泄检测 | 敏感文件/内网访问 |
| P1/D1 | `data_exfil` | 敏感文件读取 | 40+ 敏感路径匹配 |
| L1/L2 | `data_exfil` | 内网访问检测 | IP/域名模式匹配 |
| L3 | `privilege_escalation` | 凭证文件访问 | 凭证路径匹配 |

### 敏感文件路径示例

- `/etc/shadow`, `/etc/passwd`, `/etc/sudoers`
- `id_rsa`, `id_ed25519`, `.pem`, `.key`
- `.aws/credentials`, `.kube/config`, `.docker/config`
- `.env`, `credentials.json`, `api_key`

### 恶意命令示例

- 提权：`sudo su -`, `chmod +s`, `chmod 4755`
- 反弹 shell：`nc -e`, `bash -i >`, `python socket connect`
- 数据外泄：`curl -d @`, `scp user@host`
- 容器逃逸：`docker run --privileged`, `nsenter`

## 风险级别

| 级别 | 处理策略 |
|------|----------|
| **HIGH** | 立即阻止操作，计入熔断计数 |
| **MEDIUM** | 记录警告，不阻止 |
| **LOW** | 记录警告，不阻止 |

## 熔断机制

当单个会话连续触发 `CALLISTO_THRESHOLD`（默认 3）个 HIGH 风险告警时，熔断器会自动打开，阻止后续所有操作。

熔断状态：
- **CLOSED**：正常状态
- **OPEN**：已熔断，阻止所有操作

**注意**: 当前实现不支持 `HALF_OPEN` 状态。熔断后需要重置会话才能恢复。

## 故障排除

### Plugin 未加载

检查已加载的插件：

```bash
openclaw plugins list
```

确认 `callisto-plugin` 状态为 `loaded`。

### Python 依赖缺失

```bash
cd ~/.openclaw/extensions/callisto-plugin
source .venv/bin/activate  # 如果使用虚拟环境
pip install -e .
```

### 检测不生效

1. 检查插件日志：
   ```bash
   cat /tmp/callisto-plugin.log
   cat /tmp/callisto-python.log
   ```

2. 验证 Python 后端：
   ```bash
   echo '{"tool_name":"exec","parameters":{"command":"cat /etc/passwd"},"session_id":"test"}' | \
     python3 openclaw_plugin/callisto-skill/python/callisto_agent.py detect
   ```

3. 重启 OpenClaw：
   ```bash
   pkill -f openclaw
   openclaw gateway --force
   ```

### 缓存问题

如果修改了插件代码但不生效，需要完全重启 OpenClaw：

```bash
pkill -9 -f openclaw
rm -rf ~/Library/Caches/openclaw 2>/dev/null
openclaw gateway --force
```

## 项目结构

```
callisto-plugin/
├── openclaw.plugin.json       # OpenClaw 插件清单
├── package.json               # npm 包配置
├── tsconfig.json              # TypeScript 配置
├── index.ts                   # Plugin 入口（TypeScript 源码）
├── dist/
│   └── index.js               # 编译后的 Plugin（实际加载）
├── openclaw_plugin/
│   └── callisto-skill/
│       ├── package.json       # Skill 配置
│       ├── SKILL.md           # Skill 定义
│       ├── README.md          # Skill 使用指南
│       ├── src/
│       │   └── index.js       # Skill 入口（Node.js）
│       └── python/
│           └── callisto_agent.py  # Python 检测引擎
├── scripts/                   # Python 脚本工具
├── integration_examples/      # 集成示例
├── test_reports/              # 测试报告
└── docs/                      # 文档
```

## 开发

```bash
# 构建 TypeScript
npm run build

# 测试 Python 后端
echo '{"tool_name":"exec","parameters":{"command":"ls"},"session_id":"test"}' | \
  python3 openclaw_plugin/callisto-skill/python/callisto_agent.py detect
```

## 调试日志

插件运行时会在以下位置写入调试日志：

| 日志文件 | 内容 |
|---------|------|
| `/tmp/callisto-plugin.log` | Plugin hook 调用日志 |
| `/tmp/callisto-python.log` | Python 检测引擎日志 |

## License

MIT
